version: '3.8'

services:
  web:
    build: .
    container_name: nginx-web-app
    ports:
      - "8080:80"
    restart: unless-stopped
    labels:
      - "description=Simple Nginx web server in Docker"
